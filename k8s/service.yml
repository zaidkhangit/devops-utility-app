kind: Service
apiVersion: v1
metadata:
  name: app-service
  namespace: utilityapp
spec:
  selector:
    app: utility
  type: NodePort
  ports:
    - name: https
      port: 8081
      targetPort: 8081
      nodePort: 30081


